[user]
	name = nocis
	email = nocis@github.com
[core]
	autocrlf = input
[merge]
        tool = vim_mergetool
        conflictstyle = diff3

[mergetool "vim_mergetool"]
        cmd = nvim -f -c "MergetoolStart" "$MERGED" "$BASE" "$LOCAL" "$REMOTE"
        trustExitCode = true
[pager]
	log = true
